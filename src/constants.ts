import { Peer } from "./redux/slices/rendererSlice";

export const DEFAULT_SERVERS = ["https://api.mtgatool.com"];
export const DEFAULT_PEERS: Peer[] = [{ host: "api.mtgatool.com", port: 443 }];

export const dummy = true;
